{
  "name": "shopify-app-template-node",
  "version": "1.0.0",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "debug": "node --inspect-brk index.js",
    "dev": "cross-env NODE_ENV=development nodemon -r dotenv/config index.js --ignore ./frontend dotenv_config_path=./.env",
    "serve": "cross-env NODE_ENV=production node index.js",
    "firebase": "firebase emulators:start --import=./firestore --export-on-exit"
  },
  "type": "module",
  "engines": {
    "node": ">=14.13.1"
  },
  "dependencies": {
    "@shopify/shopify-app-express": "^2.1.3",
    "@shopify/shopify-app-session-storage-sqlite": "^1.2.3",
    "@tkashiro/shopify-app-session-storage-firestore": "^1.0.0",
    "compression": "^1.7.4",
    "cross-env": "^7.0.3",
    "firebase-admin": "^11.10.1",
    "serve-static": "^1.14.1"
  },
  "devDependencies": {
    "dotenv": "^16.3.1",
    "nodemon": "^2.0.15",
    "prettier": "^2.6.2",
    "pretty-quick": "^3.1.3"
  }
}
